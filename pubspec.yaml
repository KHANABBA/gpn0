name: goodpeople_news
description: A WebView app for GoodPeople News
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.0.7

flutter:
  uses-material-design: true
